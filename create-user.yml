---
-
 hosts: all
 become: false
 gather_facts: no
 tasks:
         - name: create a user called ahmed
           user:
                   name: user
                   state: present
                   comment: hello this is ahmed
                   password: "{{'password'}}"

